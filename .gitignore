# Crypto materials — NEVER commit private keys to the repo
crypto-config/
channel-artifacts/

# Secret and AES keys — managed at runtime, never committed
*.key
*.pem
*_sk
.aes_key
secret.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.env

# Go
vendor/
*.test

# Docker
.docker/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Logs
*.log
logs/

# Benchmark results (committed manually for thesis appendix)
# results/  <- intentionally NOT ignored, so evidence can be committed

# fabric-samples submodule (populated at runtime)
fabric-samples/
